#include "main.h"
/**
 * puts2 - a function that prints every other character of a string
 * @str: an input string
 * Return: void
 */
void puts2(char *str)
{
	int length = 0;
	int i = 0;

	while (str[lentgh] != '\0')
		length++;
	length -= 1;

	for (; i <= lentgh; i += 2)
		_putchar(str[i]);
	_putchar('\n');
}
