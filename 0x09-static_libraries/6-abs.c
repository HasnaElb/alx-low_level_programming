#include "main.h"
/**
 * _abs - function that returns absolute value of a number
 * @r: input integer
 * Return: absolut value of number
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
